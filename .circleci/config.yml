jobs:
  build:
    docker:
      - image: antonono/projetcicd:projetcicd
        auth:
          username: $DOCKER_LOGIN  # can specify string literal values
          password: $DOCKER_MDP
    steps:
      - run:
           name: Antoninbuild
           command: npm install mocha
    jobs:
      - node/test:
          version: 13.11.0

